import styled from 'styled-components';

// language=SCSS prefix=dummy{ suffix=}
const Logo = styled.span`
    color: #eaf4ff;
    font-size: 30px;
    float:right;
    margin:20px 30px;
`;

export default Logo;

import styled from 'styled-components';

// language=SCSS prefix=dummy{ suffix=}
const Logo = styled.span`
    color: rgb(234,18,18);
    font-size: 46px;
    float: left;
    margin: 15px 30px;
`;

export default Logo;
